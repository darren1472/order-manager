# public/_redirects (build 時にコピーされます)
# API リクエストをサーバーレス関数へ
/api    /.netlify/functions/api    200
/api/*  /.netlify/functions/api/:splat  200

# 静的ファイルはそのまま返す
/*.*   /*   200

# それ以外は index.html（SPA フォールバック）
/*     /index.html  200
